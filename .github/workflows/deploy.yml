name: DevFlow Deployment

on:
  push:
    branches:
      - main

  workflow_dispatch:
    inputs:
      template:
        description: "Template to deploy"
        required: true
        default: "nodejs"

env:
  TEMPLATE: ${{ github.event.inputs.template }}

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup SSH
        run: |
          echo "${{ secrets.EC2_KEY }}" > key.pem
          chmod 400 key.pem

      # =========================
      # 🚀 NODE.JS API DEPLOYMENT
      # =========================
      - name: Deploy Node App
        if: env.TEMPLATE == 'nodejs'
        run: |
          scp -o StrictHostKeyChecking=no -i key.pem -r templates/nodejs-api/app ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:~

          ssh -o StrictHostKeyChecking=no -i key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
          
          cd ~/app
          source ~/.bashrc
          nvm use 16

          npm install
          pkill node || true

          nohup node index.js > app.log 2>&1 &

          # 🔥 SWITCH NGINX TO NODE (SAFE VERSION)
          sudo bash -c "echo '
server {
    listen 80 default_server;
    server_name _;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host \$host;
        proxy_set_header X-Real-IP \$remote_addr;
    }
}
' > /etc/nginx/conf.d/app.conf"

          sudo systemctl restart nginx
          
          EOF

      # =========================
      # 🌐 STATIC WEBSITE DEPLOYMENT
      # =========================
      - name: Deploy Static Website
        if: env.TEMPLATE == 'static'
        run: |
          ssh -o StrictHostKeyChecking=no -i key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
          
          sudo rm -rf /usr/share/nginx/html/*
          mkdir -p ~/static-site
          
          EOF

          scp -o StrictHostKeyChecking=no -i key.pem -r templates/static-site/* ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:~/static-site/

          ssh -o StrictHostKeyChecking=no -i key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
          
          sudo cp -r ~/static-site/* /usr/share/nginx/html/

          # 🔥 REMOVE NODE PROXY CONFIG
          sudo rm -f /etc/nginx/conf.d/app.conf

          sudo systemctl restart nginx
          
          EOF

      # =========================
      # ⚙️ WORKER DEPLOYMENT
      # =========================
      - name: Deploy Background Worker
        if: env.TEMPLATE == 'worker'
        run: |
          scp -o StrictHostKeyChecking=no -i key.pem -r templates/worker ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:~

          ssh -o StrictHostKeyChecking=no -i key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
          
          pkill -f worker.js || true
          cd ~/worker

          nohup node worker.js > worker.log 2>&1 &
          
          EOF